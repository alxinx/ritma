section(
  class="py-24 bg-slate-50 dark:bg-zinc-900/30"
)
  div(
    class="max-w-7xl mx-auto px-6"
  )

    div(
      class="flex flex-col md:flex-row justify-between items-end mb-12 gap-6"
    )
      div
        h2(
          class="font-display font-extrabold text-4xl uppercase mb-4"
        )
          | Trending 
          span(class="text-ritma-cyan") Tracks

        p(
          class="text-slate-500 dark:text-slate-400 max-w-md"
        )
          | Lo más sonado en las pistas de Latinoamérica esta semana.

      a(
        class="text-primary font-bold flex items-center gap-2 hover:underline"
        href="#"
      )
        | VER TODAS 
        span(class="material-icons-round") arrow_forward

    div(
      class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8"
    )

      // -------- Track 01 --------
      div(
        class="group bg-white dark:bg-surface-dark p-5 rounded-sm border border-slate-200 dark:border-white/5 hover:border-primary/50 transition-all duration-300"
      )
        div(
          class="flex items-center gap-4 mb-6"
        )
          div(
            class="w-16 h-16 bg-ritma-orange rounded-sm flex items-center justify-center text-black font-black text-2xl"
          ) 01

          div
            h3(
              class="font-display font-bold text-lg leading-tight"
            ) PERREO FUTURISTA

            p(
              class="text-sm text-ritma-cyan"
            ) DJ ELÉCTRICO

        div(
          class="h-12 flex items-center gap-[2px]"
        )
          div(
            class="w-full bg-slate-200 dark:bg-white/10 h-1 rounded-full relative overflow-hidden"
          )
            div(
              class="absolute inset-0 bg-primary w-2/3"
            )

          span(
            class="text-[10px] text-slate-400 font-mono"
          ) 03:45

        div(
          class="mt-4 flex justify-between items-center text-xs text-slate-400 uppercase tracking-widest font-bold"
        )
          span 128 BPM

          div(class="flex gap-4")
            span(
              class="material-icons-round text-sm cursor-pointer hover:text-white"
            ) favorite_border
            span(
              class="material-icons-round text-sm cursor-pointer hover:text-white"
            ) share

      // -------- Track 02 --------
      div(
        class="group bg-white dark:bg-surface-dark p-5 rounded-sm border border-slate-200 dark:border-white/5 hover:border-primary/50 transition-all duration-300"
      )
        div(
          class="flex items-center gap-4 mb-6"
        )
          div(
            class="w-16 h-16 bg-ritma-purple rounded-sm flex items-center justify-center text-black font-black text-2xl"
          ) 02

          div
            h3(
              class="font-display font-bold text-lg leading-tight"
            ) SALSA TECH CORE

            p(
              class="text-sm text-ritma-purple"
            ) RITMA LABS

        div(
          class="h-12 flex items-center gap-[2px]"
        )
          div(
            class="w-full bg-slate-200 dark:bg-white/10 h-1 rounded-full relative overflow-hidden"
          )
            div(
              class="absolute inset-0 bg-primary w-1/2"
            )

          span(
            class="text-[10px] text-slate-400 font-mono"
          ) 04:12

        div(
          class="mt-4 flex justify-between items-center text-xs text-slate-400 uppercase tracking-widest font-bold"
        )
          span 132 BPM

          div(class="flex gap-4")
            span(
              class="material-icons-round text-sm cursor-pointer hover:text-white"
            ) favorite_border
            span(
              class="material-icons-round text-sm cursor-pointer hover:text-white"
            ) share

      // -------- Track 03 --------
      div(
        class="group bg-white dark:bg-surface-dark p-5 rounded-sm border border-slate-200 dark:border-white/5 hover:border-primary/50 transition-all duration-300"
      )
        div(
          class="flex items-center gap-4 mb-6"
        )
          div(
            class="w-16 h-16 bg-ritma-blue rounded-sm flex items-center justify-center text-black font-black text-2xl"
          ) 03

          div
            h3(
              class="font-display font-bold text-lg leading-tight"
            ) CUMBIA GALÁCTICA

            p(
              class="text-sm text-ritma-blue"
            ) SONIDO 3000

        div(
          class="h-12 flex items-center gap-[2px]"
        )
          div(
            class="w-full bg-slate-200 dark:bg-white/10 h-1 rounded-full relative overflow-hidden"
          )
            div(
              class="absolute inset-0 bg-primary w-3/4"
            )

          span(
            class="text-[10px] text-slate-400 font-mono"
          ) 05:20

        div(
          class="mt-4 flex justify-between items-center text-xs text-slate-400 uppercase tracking-widest font-bold"
        )
          span 124 BPM

          div(class="flex gap-4")
            span(
              class="material-icons-round text-sm cursor-pointer hover:text-white"
            ) favorite_border
            span(
              class="material-icons-round text-sm cursor-pointer hover:text-white"
            ) share
