header(class="relative pt-40 pb-32 overflow-hidden bg-background-dark")
  div(class="absolute inset-0 pixel-grid opacity-20 pointer-events-none")

  div(class="max-w-7xl mx-auto px-6 relative z-10")
    div(class="flex items-center gap-4 mb-8")
      div(class="h-px w-12 bg-primary")
      span(class="text-primary font-display font-bold uppercase tracking-[0.3em] text-sm")
        | Nuestra Historia

    h1(class="font-display font-extrabold text-6xl md:text-8xl leading-none uppercase mb-12")
      | NUESTRA
      br
      span(class="text-primary italic") VISIÓN

    div(class="grid lg:grid-cols-2 gap-12")
      p(class="text-2xl md:text-3xl font-body leading-tight text-slate-300")
        | Nos convertiremos en la 
        span(class="text-white font-bold")
          | plataforma de soporte multimedia más importante de Latinoamérica
        | , impulsando la infraestructura digital que define el futuro de la cultura de club.

      div(class="flex flex-col justify-end space-y-4")
        div(class="vu-meter h-12 mb-4")
          div(class="vu-bar h-8")
          div(class="vu-bar h-12")
          div(class="vu-bar h-6")
          div(class="vu-bar h-10")
          div(class="vu-bar h-4")
          div(class="vu-bar h-9")
          div(class="vu-bar h-12")
          div(class="vu-bar h-7")

        p(class="text-slate-400 max-w-md")
          | Desde el 2025, RITMA nace para cerrar la brecha técnica en la región, proporcionando herramientas de grado profesional para los arquitectos del ritmo latino.
