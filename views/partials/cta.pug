section(
  class="py-24 relative overflow-hidden"
)
  div(
    class="absolute inset-0 bg-primary pointer-events-none"
  )

  div(
    class="absolute inset-0 pixel-grid opacity-10 pointer-events-none"
  )

  div(
    class="max-w-5xl mx-auto px-6 text-center relative z-10"
  )
    h2(
      class="font-display font-black text-4xl md:text-6xl text-black uppercase mb-8"
    )
      | ¿LISTO PARA LLEVAR TU SONIDO AL SIGUIENTE NIVEL?


    a(href="/acceso/" class="bg-black text-white px-12 py-5 font-display font-extrabold text-xl uppercase rounded hover:scale-105 transition-transform shadow-2xl") 
      | ÚNETE A LA REVOLUCIÓN
