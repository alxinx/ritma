section(class="py-24 relative overflow-hidden bg-primary")
  //- El pixel grid ahora contrasta con el fondo amarillo
  div(class="absolute inset-0 pixel-grid opacity-20 pointer-events-none")
  
  //- Decoración geométrica sutil para dar profundidad
  div(class="absolute -left-20 -top-20 w-64 h-64 bg-black/5 rounded-full blur-3xl")
  div(class="absolute -right-20 -bottom-20 w-64 h-64 bg-white/20 rounded-full blur-3xl")

  div(class="max-w-5xl mx-auto px-6 text-center relative z-10")
    //- Badge superior para contexto
    div(class="inline-block px-4 py-1 bg-black text-primary text-[10px] font-bold uppercase tracking-[0.3em] mb-8")
      | Limited Access Beta

    h2(class="font-display font-black text-4xl md:text-7xl text-black uppercase mb-10 leading-[0.9] tracking-tighter")
      | ¿LISTO PARA LLEVAR TU SONIDO AL 
      span(class="block text-transparent stroke-black stroke-1" style="-webkit-text-stroke: 2px black;") SIGUIENTE NIVEL?

    div(class="flex flex-col items-center gap-6")
      a(
        href="/acceso/" 
        class="bg-black text-white px-12 py-6 font-display font-extrabold text-xl uppercase rounded-sm hover:scale-105 active:scale-95 transition-all shadow-[0_20px_50px_rgba(0,0,0,0.3)] flex items-center gap-4"
      ) 
        | ÚNETE A LA REVOLUCIÓN
        span(class="material-icons-round") rocket_launch

      p(class="text-black/60 font-display font-bold text-[10px] uppercase tracking-widest")
        | Registro gratuito para usuarios seleccionados