main(class="flex-1 max-w-7xl mx-auto px-6 py-24 md:py-32 relative")
  //- Elemento decorativo: Gradiente de fondo sutil
  div(class="absolute top-0 left-1/2 -translate-x-1/2 w-full h-96 bg-primary/5 blur-[120px] pointer-events-none")

  div(class="flex flex-col items-center text-center mb-24 relative z-10")
    
    //- Badge de Categoría
    div(class="flex items-center gap-4 mb-8")
      div(class="h-[1px] w-12 bg-primary/40")
      span(class="text-primary text-[10px] font-bold tracking-[0.5em] uppercase")
        | SISTEMA OPERATIVO PARA DJS
      div(class="h-[1px] w-12 bg-primary/40")

    //- Título Monumental
    h1(class="text-6xl md:text-8xl font-display font-black tracking-tighter mb-10 leading-[0.9] text-white uppercase")
      | ESTRUCTURA 
      span(class="text-primary italic hover:glow-primary transition-all") RITMA

    //- Descripción
    p(class="max-w-2xl text-slate-400 text-lg md:text-xl leading-relaxed font-body")
      | Una infraestructura digital robusta diseñada exclusivamente para potenciar el flujo de trabajo de los 
      span(class="text-white font-medium") DJs con swing latino. 
      | Un ecosistema cerrado de alto rendimiento que redefine la gestión musical.

  //- Espacio para el Grid de Arquitectura (Sugerencia de continuación)
  div(class="grid md:grid-cols-2 lg:grid-cols-3 gap-px bg-white/5 border border-white/5")
    //- Aquí irían los bloques de la estructura técnica que definamos