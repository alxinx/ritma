div(
  class="w-full max-w-[560px] bg-white/[0.02] border border-white/10 p-8 md:p-14 rounded-2xl glow-card backdrop-blur-xl relative overflow-hidden text-center"
)
  div(class="absolute top-0 left-0 w-full h-1 pixel-bg opacity-20")

  //- HEADER
  div(class="flex flex-col items-center mb-8")
    div(class="relative mb-8")
      div(class="absolute inset-0 bg-primary/20 blur-2xl rounded-full pulse-glow")
      div(
        class="size-24 bg-primary/10 rounded-full flex items-center justify-center border border-primary/40 relative z-10 shadow-[0_0_30px_rgba(201,218,43,0.2)]"
      )
        span(class="material-symbols-outlined text-primary text-6xl")
          | check_circle

    h1(
      class="text-white text-3xl md:text-4xl font-montserrat font-bold tracking-tight mb-6 uppercase"
    )
      | ¡SOLICITUD RECIBIDA!

    p(
      class="text-white/60 font-roboto text-base leading-relaxed max-w-md mx-auto"
    )
      | Tu solicitud está siendo procesada por nuestro equipo técnico.
      | En breve recibirás un mensaje por WhatsApp o correo electrónico con los siguientes pasos.

  //- SYSTEM STATUS
  div(class="my-10 space-y-4")
    div(
      class="flex justify-between items-center text-[10px] font-bold tracking-[0.2em] text-primary/60"
    )
      span ESTADO DEL SISTEMA
      span(class="flex items-center gap-2")
        span(
          class="size-2 bg-primary rounded-full animate-pulse shadow-[0_0_8px_#C9DA2B]"
        )
        | UNDER REVIEW

    div(
      class="h-1.5 w-full bg-white/5 rounded-full overflow-hidden relative"
    )
      div(
        class="absolute top-0 left-0 h-full w-[65%] bg-primary rounded-full relative"
      )
        div(class="absolute inset-0 progress-shimmer")

  //- ACTION
  div(class="pt-6 flex flex-col gap-6")
    a(href="/" class="w-full border border-white/10 hover:bg-white/5 text-white h-16 rounded-lg font-montserrat font-bold text-sm tracking-[0.2em] transition-all transform hover:scale-[1.01] active:scale-[0.98] flex items-center justify-center gap-3 group" ) 
    
      span(
        class="material-symbols-outlined text-xl transition-transform group-hover:-translate-x-1"
      )
        | arrow_back
      span VOLVER AL INICIO

  //- SESSION FOOTER
  div(class="mt-12 flex justify-between items-center opacity-30")
    div(class="flex gap-1")
      div(class="h-[1px] w-8 bg-primary")
      div(class="h-[1px] w-2 bg-primary")

    span(
      class="text-[9px] tracking-[0.4em] font-bold text-primary/80 font-mono"
    )
      | SESSION_ID: RTM_774_ACK

    div(class="flex gap-1")
      div(class="h-[1px] w-2 bg-primary")
      div(class="h-[1px] w-8 bg-primary")
