div(class="stats-crystal glass-card")
  div(class="absolute inset-0 pixel-pattern opacity-5")
  div(class="flex justify-between items-start relative z-10")
    span(class="text-[11px] font-bold text-gray-400 uppercase tracking-[0.3em]") Cr&eacute;ditos Gastados
    span(class="material-symbols-outlined") payments
  div(class="mt-4 relative z-10")
    span#up-stat-creditos-gast(class="text-4xl font-display font-extrabold text-white") #{creditosGastados || 0}
    div(class="mt-1 flex items-center gap-1.5 text-[10px] font-mono")
      span(class="text-white/40") TOTAL
