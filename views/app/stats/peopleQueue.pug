div(class="glass-card rounded-3xl p-8 relative overflow-hidden group")
  div(class="absolute inset-0 pixel-pattern opacity-5")
  div(class="relative z-10 flex flex-col h-full justify-between")
    div(class="flex justify-between items-start")
      h3(class="text-[11px] font-bold text-primary uppercase tracking-[0.3em]") Aspirantes en Espera
      span(class="material-symbols-outlined text-primary text-3xl animate-pulse") pending_actions
    div(class="mt-8 flex items-baseline gap-4")
      span(class="text-7xl font-display  font-extrabold text-white tracking-tighter") 42
      span(class="text-xs font-bold text-primary/60 uppercase") Pendientes por revisar
    div(class="mt-6 flex gap-2")
      span(class="tag-primary") Necesitas revisarlos y autorizarlos 🧐
