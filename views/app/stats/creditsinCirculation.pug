div(class="glass-card rounded-3xl p-10 relative overflow-hidden group flex items-center justify-between border-primary/20 bg-primary/5")
  
  div(class="absolute -right-8 -bottom-8 opacity-5")
    span(class="material-symbols-outlined text-[200px] text-primary") account_balance_wallet

  div(class="relative z-10")
    h3(class="text-[11px] font-bold text-primary uppercase tracking-[0.4em] mb-4") Créditos en Circulación
    
    div(class="flex items-baseline gap-2")
      span(class="text-7xl font-display font-bold tracking-tighter text-white") 450,282
      span(class="text-xl font-accent font-bold text-primary") R$
    
    p(class="mt-4 text-[10px] text-gray-500 font-mono tracking-widest uppercase bg-black/40 px-3 py-1.5 rounded-md inline-block")
      | Total de créditos que usuarios han comprado y no han consumido.

  div(class="relative z-10 flex flex-col gap-2")
    div(class="h-16 w-16 rounded-2xl border border-primary/20 flex items-center justify-center bg-black/40")
      span(class="material-symbols-outlined text-primary text-3xl") token
