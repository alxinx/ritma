footer(class="py-12 border-t border-white/5 bg-background-dark/80 backdrop-blur-md relative overflow-hidden")
  //- Efecto de línea de escaneo sutil al fondo
  div(class="absolute inset-0 bg-gradient-to-b from-transparent via-white/5 to-transparent h-px w-full top-0 animate-pulse")

  div(class="max-w-7xl mx-auto px-6 flex flex-col md:flex-row justify-between items-center gap-8 relative z-10")

    //- Logo / Branding
    div(class="flex items-center gap-3 opacity-60 hover:opacity-100 transition-opacity group cursor-default")
      span(class="material-symbols-outlined text-2xl text-primary group-hover:rotate-12 transition-transform") bolt
      span(class="font-display font-black italic tracking-tighter text-xl text-white") RITMA

    //- Status & Credits
    div(class="flex flex-wrap justify-center gap-8 text-[10px] font-bold tracking-[0.3em] text-white/30 uppercase font-display")
      span(class="flex items-center gap-2 text-primary")
        span(class="size-1.5 bg-primary rounded-full animate-pulse shadow-[0_0_8px_#C9DA2B]")
        | SYSTEM_LIVE

      span(class="hover:text-white transition-colors cursor-default") DESARROLLADO POR CODREAM
      span(class="cursor-default") © 2026

    //- Social / Actions
    div(class="flex gap-4")
      a(
href="#"
class="size-10 rounded-sm bg-white/5 flex items-center justify-center border border-white/5 hover:bg-primary hover:text-black hover:border-primary transition-all duration-300 group"
      )
        span(class="material-symbols-outlined text-lg") share

      a(
        href="mailto:hola@ritma.com"
        class="size-10 rounded-sm bg-white/5 flex items-center justify-center border border-white/5 hover:bg-primary hover:text-black hover:border-primary transition-all duration-300 group"
      )
        span(class="material-symbols-outlined text-lg") mail