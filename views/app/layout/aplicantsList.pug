div(class="glass-card rounded-3xl p-8 flex flex-col")
  h3(class="text-[11px] font-bold text-primary uppercase tracking-[0.3em] mb-4") Solicitudes Pendientes

  div(id="aspirantes-list" class="space-y-3 overflow-y-auto custom-scrollbar max-h-[600px]")
    //- Rendered by JS

  div(id="aspirantes-empty" class="text-center py-10 text-white/30 hidden")
    span(class="material-symbols-outlined text-4xl mb-2") inbox
    p(class="text-xs uppercase tracking-widest") Sin solicitudes pendientes
